import React from "react";
import ReactDOM from "react-dom/client";

//REACT ELEMENT
const JSXheading = <h1>Hello React basic breakdown</h1>;

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(JSXheading);
